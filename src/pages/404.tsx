import React from 'react';
import NotFoundScreen from '../components/screens/NotFounScreen';

const NotFound = (): React.ReactElement => {
  return (
    <NotFoundScreen />
  );
};

export default NotFound;
